---
// Wordmark — DESIGN_SYSTEM v3 (brand casing D3, unchanged). Circular oxblood mark +
// "Somnary" logotype in Instrument Sans + oxblood period. Capitalized "Somnary." is the
// ratified mark (D3) — do NOT lowercase.
// Linked → wraps in <a> (focus ring applies); mark is decorative (aria-hidden).
interface Props {
  href?: string;
}
const { href = '/' } = Astro.props;
---

<a class="wordmark" href={href} aria-label="Somnary — home">
  <span class="mark" aria-hidden="true">S</span>
  <span class="logotype">Somnary<span class="dot">.</span></span>
</a>

<style>
  .wordmark {
    display: inline-flex;
    align-items: center;
    gap: var(--sp-2);
    text-decoration: none;
  }
  /* filled oxblood disc with a white cap — prototype .brand-mark */
  .mark {
    display: grid;
    place-items: center;
    width: 27px;
    height: 27px;
    flex: none;
    border-radius: var(--r-pill);
    background: var(--primary);
    color: #ffffff;
    font-family: var(--font-display);
    font-weight: 700; /* Instrument Sans caps at 700 */
    font-size: 14px;
    line-height: 1;
  }
  .logotype {
    font-family: var(--font-display);
    font-weight: 700; /* Instrument Sans caps at 700 */
    font-size: 27px;
    letter-spacing: -0.04em;
    color: var(--ink);
    line-height: 1;
  }
  /* trailing-period dot in oxblood --primary (D3 mark) */
  .dot {
    color: var(--primary);
  }
</style>
