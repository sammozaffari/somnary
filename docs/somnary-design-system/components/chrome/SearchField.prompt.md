The site's primary object. Hero size on home, `size="sm"` in headers.

```jsx
<SearchField onSubmit={(q) => go(q)} />
```

Button copy is "Look it up" — sentence case, plain. Geometry is concentric: inner radius = outer radius − padding (see the nested-radii rule in tokens/spacing.css).
