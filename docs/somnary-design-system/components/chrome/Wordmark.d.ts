/** The text wordmark — "Somnary", capitalised, no trailing period (locked decision). Onest 600. */
export interface WordmarkProps {
  /** Font size in px. Default 28. */
  size?: number;
  style?: React.CSSProperties;
}
